# Python / build
.venv
.vscode
__pycache__/

*.pyc

# live watcher singleton lock
.watch.pid

# in-flight tailor passes (only the final accepted pass lands in output/)
.tailor_cache/
# per-run JSONL logs
logs/
# debug prompt dumps (getPrompt.py / any future debug_dump)
sendPrompt*.txt
receivePrompt*.txt

# LaTeX build artifacts
*.aux
*.log
*.out
*.fls
*.fdb_latexmk
*.synctex.gz
*.toc
*.bbl
*.blg
missfont.log
diff/
jobDescription/
output/
output_Low/
dataset/

# Sandbox-masked home dotfiles (not part of this project)
.bashrc
.bash_profile
.profile
.zshrc
.zprofile
.gitconfig
.gitmodules
.mcp.json
.ripgreprc
.idea
.claude/agents
.claude/commands
.claude/hooks
.claude/settings.json
# scrape-jobs skill: local browser profile + discovery dumps
.claude/skills/scrape-jobs/.profile/
.claude/skills/scrape-jobs/.debug/
.claude/skills/scrape-jobs/last_run.json

# Secrets
.env

