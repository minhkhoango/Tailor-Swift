Talk like caveman, always. 
When code python, pylance strict type hint. Use fewer .py files with big functions do many things > many .py files with tiny functions do 1 tiny thing
On big changes, git worktree